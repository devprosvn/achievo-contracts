
{
  "name": "achievo_contract",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "type": "module",
  "scripts": {
    "build": "near-sdk-js build src/contract.ts build/achievo.wasm",
    "test": "$npm_execpath run build && ava -- ./build/achievo.wasm",
    "deploy:testnet": "near deploy --wasmFile build/achievo.wasm --accountId your-account.testnet"
  },
  "dependencies": {
    "near-sdk-js": "2.0.0"
  },
  "devDependencies": {
    "ava": "^6.1.3",
    "near-workspaces": "^4.0.0",
    "typescript": "^5.4.5"
  },
  "ava": {
    "files": [
      "sandbox-test/*.ava.js"
    ]
  }
}
